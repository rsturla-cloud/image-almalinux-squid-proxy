name: almalinux-squid-proxy
platforms:
  - amd64
source_tags:
  - 9
tags:
  - ${OS_VERSION}
  - ${OS_VERSION}-${COMMIT_SHA_SHORT}
  - ${OS_VERSION}-${BUILD_TIMESTAMP_SHORT}
  - ${OS_VERSION}-${BUILD_TIMESTAMP_SHORT}-${COMMIT_SHA_SHORT}
